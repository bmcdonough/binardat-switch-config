version: '3.8'

# Example 1: Single switch with environment variables
services:
  ssh-enabler:
    image: ghcr.io/bmcdonough/binardat-switch-config:latest
    environment:
      - SWITCH_IP=${SWITCH_IP:-192.168.2.1}
      - SWITCH_USERNAME=${SWITCH_USERNAME:-admin}
      - SWITCH_PASSWORD=${SWITCH_PASSWORD:-admin}
      - SWITCH_SSH_PORT=${SWITCH_SSH_PORT:-22}
    network_mode: host

# Example 2: Single switch with environment file
# Uncomment to use
#
# services:
#   ssh-enabler:
#     image: ghcr.io/bmcdonough/binardat-switch-config:latest
#     env_file:
#       - .env
#     network_mode: host

# Example 3: Multiple switches
# Uncomment to use
#
# services:
#   switch-1:
#     image: ghcr.io/bmcdonough/binardat-switch-config:latest
#     environment:
#       - SWITCH_IP=192.168.2.1
#       - SWITCH_USERNAME=admin
#       - SWITCH_PASSWORD=admin
#     network_mode: host
#
#   switch-2:
#     image: ghcr.io/bmcdonough/binardat-switch-config:latest
#     environment:
#       - SWITCH_IP=192.168.2.2
#       - SWITCH_USERNAME=admin
#       - SWITCH_PASSWORD=admin
#     network_mode: host
#
#   switch-3:
#     image: ghcr.io/bmcdonough/binardat-switch-config:latest
#     environment:
#       - SWITCH_IP=192.168.2.3
#       - SWITCH_USERNAME=admin
#       - SWITCH_PASSWORD=admin
#     network_mode: host
