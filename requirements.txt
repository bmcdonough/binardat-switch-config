# Runtime dependencies
# NOTE: pyproject.toml is the source of truth for dependencies.
# This file is maintained for compatibility with tools that don't support pyproject.toml.
# To update dependencies, edit [project.dependencies] in pyproject.toml and regenerate this file.
#
# Generate with: pip-compile pyproject.toml (requires pip-tools)
# Or manually sync with pyproject.toml

# Web communication
requests>=2.31.0

# HTML parsing
beautifulsoup4>=4.12.0

# Cryptography (RC4 encryption for switch authentication)
pycryptodome>=3.19.0

# YAML configuration
pyyaml>=6.0
